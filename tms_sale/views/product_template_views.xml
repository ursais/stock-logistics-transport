<odoo>
    <record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.template.common.form.inherit.tms.product</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_policy']" position="after">
                <field name="tms_trip" invisible="1" />
                <field
                    name="tms_tracking"
                    string="Tracking"
                    invisible="not tms_trip"
                    required="tms_trip"
                />
            </xpath>
            <xpath expr="//div[@name='pricing']" position="after">
                <field
                    name="tms_factor_type"
                    string="Factor Type"
                    invisible="not tms_trip"
                    required="tms_tracking"
                />
                <field
                    name="tms_factor_distance_uom"
                    string="Distance UoM"
                    invisible="not tms_trip or tms_factor_type != 'distance'"
                    required="tms_factor_type"
                />
                <field
                    name="tms_factor_weight_uom"
                    string="Weight UoM"
                    invisible="not tms_trip or tms_factor_type != 'weight'"
                    required="tms_factor_type"
                />
            </xpath>
        </field>
    </record>
</odoo>
